import { useEffect, useState } from "react"
import { useSelector } from ""

export default function useRedux(target) {
	const [data, setData] = useState({})
	const slice = useSelector((state) => state[target].value)
	useEffect(() => {})
	return {
		data,
	}
}
